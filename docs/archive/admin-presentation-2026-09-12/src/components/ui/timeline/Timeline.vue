<script setup lang="ts">export interface TimelineItem { id: string | number; title: string; description?: string } defineProps<{ items: TimelineItem[]; emptyText?: string }>();</script>
<template><ol v-if="items.length" class="border-l"><li v-for="item in items" :key="item.id" class="relative pb-6 pl-5 last:pb-0"><span class="absolute -left-1 top-1.5 size-2 rounded-full bg-primary" aria-hidden="true" /><div class="flex flex-wrap items-baseline justify-between gap-2"><p class="font-semibold">{{ item.title }}</p><slot name="meta" :item="item" /></div><p v-if="item.description" class="mt-1 text-sm text-muted-foreground">{{ item.description }}</p><slot name="item" :item="item" /></li></ol><p v-else class="text-sm text-muted-foreground">{{ emptyText ?? "Belum ada riwayat." }}</p></template>
