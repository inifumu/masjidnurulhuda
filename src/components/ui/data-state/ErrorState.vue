<script setup lang="ts">import { AlertCircle } from "lucide-vue-next"; import { Button } from "@/components/ui/button"; defineProps<{ title?: string; description?: string }>(); const emit = defineEmits<{ (event: "retry"): void }>();</script>
<template><section role="alert" class="border-l-2 border-destructive bg-card px-4 py-5"><div class="flex gap-3"><AlertCircle class="mt-0.5 size-5 shrink-0 text-destructive" aria-hidden="true" /><div><h3 class="font-semibold">{{ title ?? "Data gagal dimuat" }}</h3><p class="mt-1 text-sm text-muted-foreground">{{ description ?? "Coba muat ulang tanpa kehilangan pekerjaan Anda." }}</p><Button class="mt-4" variant="outline" @click="emit('retry')">Coba lagi</Button></div></div></section></template>
