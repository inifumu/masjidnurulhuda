<script setup lang="ts">import { RefreshCw } from "lucide-vue-next"; import { Button } from "@/components/ui/button"; defineProps<{ title?: string; description?: string }>(); const emit = defineEmits<{ (event: "refresh"): void }>();</script>
<template><section role="alert" class="border-l-2 border-warning bg-card px-4 py-5"><div class="flex gap-3"><RefreshCw class="mt-0.5 size-5 shrink-0 text-warning" aria-hidden="true" /><div><h3 class="font-semibold">{{ title ?? "Data telah berubah" }}</h3><p class="mt-1 text-sm text-muted-foreground">{{ description ?? "Muat data terbaru sebelum melanjutkan." }}</p><Button class="mt-4" variant="outline" @click="emit('refresh')">Muat data terbaru</Button></div></div></section></template>
